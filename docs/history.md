# uprightlab - Build History

## May 2026

### Monday 18th

Kerby is being built first. It is "new" hardware so sits under my desk whilst being built. Once Proxmox is installed, it will be moved to the homelab under the stairs. Nucky will be rebuilt in place as it is already under the stairs. Kerby will need the service key adding after being built as it will be built before the Raspberry Pi node (piey). Nucky will be built once the piey node is running and the ansible runbook and proxmox `answer.toml` are finalised during the build of kerby. At that point  nucky will not need the service key added, it will already be included in the `answer.toml`.