# Environment files and external (out-of-repo) secrets
**/.env
**/secrets-external.yaml

# Private keys — defensive, never commit (public .pub keys are unaffected)
*.pem
*.key
*.ppk
id_rsa
id_ed25519

# System
.DS_Store
